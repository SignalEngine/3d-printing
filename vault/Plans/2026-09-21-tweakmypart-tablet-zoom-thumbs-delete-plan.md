# TweakMyPart: zoom in on him with his hands on the dials, real thumbnails, delete (builder handover)

You are the BUILDER. Build only this, then stop and report. Repo `SignalEngine/printtweak`, worktree on branch `build/tablet-zoom-thumbs`, cut from `origin/master` (≥ 8bc6aee). Real `node_modules` (check `[ -L node_modules ]`; `next dev --webpack`). Never touch `/root/printtweak`, the service, Convex env, or run `convex deploy`/`convex dev` against prod (anonymous codegen only). `run-limited` for heavy commands. `GATES.md` before code (`/unlazy tree N`), committed last. Load `animate`, `emil-design-eng` and `impeccable`'s `reference/craft-floor.md` before UI code. Run `python3 /root/.claude/scripts/surface-sweep.py TabletPreview SCREEN_BOX DIAL_L DIAL_R tablet.jpg frontUrl listMine PrintCard remove deletedAt --out spec/surfaces.md` and account for every hit.

## Why (James, 21 Sep 22:15, two phone screenshots)
1. Ready page: "zoom in on the guy, him looking down, his fingers on the dials, and they move as the model is moved." Today the tablet frame (`public/mascot/tablet.jpg`, 816×1054) shows him full-length with the tablet in the middle third; the dials are small grey circles.
2. My prints: "I want the model as the thumbnail, not all the same." After the Convex redeploy the cards have names/dates, but the picture tiles are empty (the front render does not show). Also "NaN undefined NaN" was the stale query — fixed by the redeploy; keep a test that the card copes with a missing quote.
3. "Be able to delete models."

## 1. Zoom and hands — `components/mascot/TabletPreview.tsx`, `app/globals.css`, new assets under `public/mascot/`
- **Crop**: in tablet mode (ready page and the working page's tablet) the frame is cropped to head-to-hips: the visible window is the region from just above his head to just below the tablet (measure on `tablet.jpg`; roughly y 12%–78%, x 8%–92%), scaled to fill the stage width. Implement as a wrapper with `overflow: hidden` and the image/videos positioned with percentage `transform: scale(k) translate(...)` so `SCREEN_BOX`, `DIAL_L`, `DIAL_R` keep working (recompute them for the cropped frame, or apply the same transform to the overlay layer so the percentages stay valid — pick the one that keeps the hotspots exact; prove it with the existing click/drag tests). The transform-in/out clips get the same crop so the entrance still lines up.
- **Looking down + fingers on the dials (new clip, generated)**: make `tab-knobs.mp4` (5 s, loop-safe: first and last frame = the tablet keyframe) with the fal.ai account used for every mascot clip — key in `/root/accelion-ai/.env.local` as `FAL_KEY` (read it in a script via `set -a; . …; set +a`; NEVER print or commit it). Model: Kling 2.x image-to-video (the same endpoint the 14 Sep clips used — see `vault/Design/tweakmypart-mascot/2026-09-14-animation-redo-plan.md` for the prompt style: beat-by-beat, "small, subtle" face, short negative prompt). Start image `vault/Design/tweakmypart-mascot/key-tablet.png` (or `public/mascot/tablet.jpg`). Prompt beats: 0–1 s he tilts his head down to look at the tablet screen, both hands resting on the side dials; 1–3 s his left hand turns the left dial a quarter turn clockwise and back; 3–5 s the right hand turns the right dial a quarter turn and back, head stays down; screen stays blank and flat; camera locked. Negative: `camera move, screen content, exaggerated smile, extra hands, morphing`. Budget: up to 3 takes (~$1.40 each); pick the one where the screen rectangle stays put (measure it per frame with a script — the DOM viewer overlays it). Save the take, its poster, and a `tab-knobs.review.md` with frame stills at 0/1/2/3/4/5 s.
- **Move with the model**: when the customer drags the screen or a dial, scrub `tab-knobs.mp4` `currentTime` from the rotation: yaw drives 1–3 s (left dial), pitch drives 3–5 s (right dial); at rest the frame sits at 0 s (head down, hands on dials). If the generated clip's dials are not usable (report why), keep the head-down first frame as the new still `tablet.jpg` and rotate only the DOM dial overlays — make those overlays look like the dials in the frame (same size, a radial highlight that rotates), not grey discs.
- Keep `prefers-reduced-motion`: still frame only.

## 2. Thumbnails — `convex/designs.ts` `listMine`, `components/prints/PrintCard.tsx`
- Find why the render is not shown on prod cards (likely: `files.front` id present but `getUrl` returns null for the legacy `files` shape, or the `<img>` is hidden behind the tile's background). Reproduce with the real prod row shapes (ask the brain for `designs.jsonl` from the scratchpad: `/tmp/claude-0/-root-3d-printing/b59e9ac3-f8b5-4a83-98d1-e5d6fbb04e3c/scratchpad/typesafe/designs.jsonl`) in a convex-test with a stored blob. Fix so ready designs show their front render (assembled render when present), object-fit cover, on the light paper tile the sketches use. Non-ready designs: the mascot avatar small in the corner of an empty paper tile, not centred large.

## 3. Delete — `convex/designs.ts`, `convex/schema.ts`, `components/prints/PrintCard.tsx`
- `designs:remove({ designId })`: owner only, sets `deletedAt` (soft; files stay), refuses while `queued`/`designing`/`checking` ("finish or fail first"). `listMine` and `designs.get` exclude deleted (get → `not_found`). Card: a small "⋯" menu → "Delete" → inline confirm ("Delete wall clamp? This can't be undone.") → removed from the grid with a 200 ms fade; toast-free. `lib/messages.ts` mapping for `busy_design`.

## Tests and proof
- Unit: crop keeps hotspots (drag on the dial rotates; screen tap still opens full view); scrub maps yaw/pitch to the clip time ranges; reduced motion = still; thumbnails render for the prod row shapes; delete flow + guards; listMine/get exclude deleted.
- Fixture `/dev/motion?state=ready` at 390×844 and 1280×800: record `spec/motion/ready-zoom-{mobile,desktop}.webm` — him cropped close, head down, dials turning as the model is dragged; `spec/motion/prints-thumbs-390.png` with real-looking renders; `prints-delete-mobile.webm`.
- `run-limited npx vitest run`, `npx tsc --noEmit`, `run-limited npx next build` green.

## Hard rules
No self-review, merge or push. Report RED/GREEN, file:line, fal spend, recording timestamps, anything not verified; Convex changes (deploy). No new dependencies. Never print or commit any key. Keep every control labelled, focus visible, tap targets ≥ 44 px.
