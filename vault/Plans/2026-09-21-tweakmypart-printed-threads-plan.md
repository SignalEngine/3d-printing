# TweakMyPart / model-forge: printed threads that actually build — thread helper + proof (builder handover)

You are the BUILDER. Two worktrees: `/root/wt-mf-thread` (repo `/root/3d-printing`, branch `build/model-forge-thread`, the skill lives in `skills/model-forge/`) and `/root/wt-pt-thread` (repo `SignalEngine/printtweak`, branch `build/printed-threads`, cut from `origin/master` ≥ ebbfd10). Real `node_modules` in the printtweak worktree (check `[ -L node_modules ]`). Never touch `/root/printtweak`, `/root/3d-printing` itself, the service, Convex env, or run `convex deploy`/`convex dev` against prod. `run-limited` for heavy commands. `GATES.md` before code (`/unlazy tree N`) in the printtweak worktree, committed last. Python for the skill: `/root/3d-printing/.venv/bin/python` (build123d installed).

## Why (James, 21 Sep 19:10: "fix all these" — item 3)
No real run has ever produced a printed thread. bd_warehouse `IsoThread` never tessellates watertight (the caddy burned $2 on it); told to sweep a coarse thread itself, the model swapped in a cross-pinned rod instead. So the mechanical gate's thread checks (pitch/starts/hand match + fit) are unit-tested only, and every "adjustable" design becomes a pin or a wedge.

## 1. A thread helper the model can call — `skills/model-forge/scripts/thread.py` (+ `references/build123d-patterns.md`, `SKILL.md`)
- `thread.py` exposes `external_thread(major_mm, pitch_mm, length_mm, starts=1, hand="right", clearance_mm=0.0, flank_deg=30, core=True)` and `internal_thread(...)` returning build123d `Part` solids that are watertight (a swept trapezoidal profile with a root chamfer, truncated crest and root so no zero-thickness edges; clearance applied to the female profile), plus `bolt(major_mm, pitch_mm, length_mm, head=("hex", af_mm, h_mm))` and `nut(...)` conveniences. Coarse by design: pitch ≥ 2.0 mm, major ≥ 8 mm, else it raises with a message that says to use a heat-set insert or a pin.
- CLI: `python3 thread.py demo --out /tmp/x` writes `bolt.3mf` + `nut.3mf` and runs `verify_model.py` on both (exit 0 required) and `fit.py` on the pair in the assembled position (CLEARANCE ≥ clearance − 0.05, never INTERFERE). This is the helper's own self-check; it must pass for M8×2.5, M10×3 and M12×3 2-start.
- `build123d-patterns.md`: replace the IsoThread section with the helper's usage; state the rule (printed threads: ≥ M8, pitch ≥ 2 mm, 0.4 mm clearance for PLA, chamfer both ends). `SKILL.md`: one line pointing at it.
- `worker/job/system_prompt.md` (printtweak): the Threads rule now says "use `/opt/model-forge/scripts/thread.py` (`external_thread`/`internal_thread`); never bd_warehouse; if the helper raises, use a pin/ratchet substitute and say so". `worker/setup_network.sh` already copies the skill into the image — confirm `thread.py` lands in `/opt/model-forge/scripts` (grep the script; adjust if it lists files).

## 2. Proof — sandbox runs (report cost/time and the files)
- Rebuild a LOCAL image tag `printtweak-job:thread` from your worktrees (never retag `latest`; `docker build` with the worktree's skill copied — read `setup_network.sh` and reproduce its copy step against `/root/wt-mf-thread/skills/model-forge`). Run the fixture request "a printed M10 bolt 40 mm long with a matching wing nut, 3 mm pitch, right-hand" through `run_job.py` as a design job (`kind: "design"`, same flags as `worker.py JOB_DOCKER_ARGS`, the worker token from the brain — ask for `g7`-style: prepare the script, the brain runs it). Expect: `parts.json` with bolt + nut, `checks.json` with a `threads` entry (pitch 3, starts 1, hand right, both sides), `assembly.json`, and the host mechanics gate (`worker/mechanics.py` from origin/master) passing the thread pair with a gap ≈ 0.4 mm. One run is enough if it passes; if it fails, fix the helper/prompt once and run again.
- Unit: `worker/tests`: prompt needle test for the helper path; skill: `skills/model-forge/tests/test_thread.py` running the demo self-check (mark slow).

## Hard rules
No self-review, merge or push. Report RED/GREEN, file:line, cost/time, the sandbox files, anything not verified. No new dependencies (build123d only). Do not change the brief job or the viewer.
