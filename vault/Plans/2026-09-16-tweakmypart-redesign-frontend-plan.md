# TweakMyPart: front-end redesign, dark studio (builder handover, part 2 of the redesign)

You are the BUILDER. Build only this, then stop and report. Repo `SignalEngine/printtweak`, worktree on branch `build/redesign-frontend`, cut from `origin/master`. Real `node_modules` (check `[ -L node_modules ]` before any npm). The site is live: never touch `/root/printtweak`, the service, Convex env, or run `convex deploy`/`convex dev` against prod (anonymous codegen only; delete `.convex/` and `.env.local` before committing). `run-limited` for heavy commands. Write `GATES.md` at the worktree root before code (`/unlazy tree N`), commit it last. Run `python3 /root/.claude/scripts/surface-sweep.py Mascot TabletPreview ProgressRail LiveLog PartDownloads glbUrl latestGlbUrl versions --out spec/surfaces.md` first and account for every hit.

## Why
James (16 Sep): the current look "is rubbish and needs a full overhaul". He chose the **dark studio** direction and edited the canvas himself. The canvas is the spec: `/tmp/claude-0/-root-3d-printing/b59e9ac3-f8b5-4a83-98d1-e5d6fbb04e3c/scratchpad/design/{Landing,Request,Main,Ready}.dc.html` (Main = the working page). Read all four before writing code; copy their layout, copy, spacing and colours. Positions James set by hand (mascot size/placement, card positions) are deliberate.

## System (from the canvas; keep `app/globals.css` as the single source)
- Ground `#0b0d10`, card `#15181d`, line `#262b33`, line-strong `#333a45`, text `#eceff2`, muted `#9ba4ae`, dim `#5f6773`, accent `#f08a3c` (buttons, current step, live cursor), focus/teal `#46c7b5` (done steps, ready state). Stage background: `radial-gradient(900px 520px at <x> <y>, #2a1a0f 0%, #15181d 55%, #0b0d10 100%)` inside a 24 px radius panel with a `#262b33` border.
- Fonts already loaded in `app/layout.tsx`: Bricolage Grotesque (display, 700/800, letter-spacing −0.02…−0.03em), Instrument Sans (body); add JetBrains Mono (400) as `--font-mono` for version labels, timings, quotes.
- Buttons: accent pill 12 px radius, `#0b0d10` text, 600 weight; secondary = `#262b33` fill. Inputs: `#0b0d10` fill, `#333a45` border, 12–16 px radius.
- Mascot: the existing `components/mascot/Mascot.tsx` video mascot stays (state machine unchanged); place and size it per canvas (it sits inside the stage panel, bottom-anchored on the working/ready stage, larger and higher on landing/request as James set). Drop shadow `0 0 60px rgba(240,138,60,.25)`.

## Pages
1. **Landing `app/page.tsx`** = `Landing.dc.html`: header (logo, How it works, Examples, Sign in, accent "Design my part"), hero stage with headline "Your part, modelled in front of you.", sub-copy, CTA + "Free while in beta · usually 3–20 min", mascot, a small tablet card showing a real recent render (use `frontUrl` of the newest ready design of the signed-in user if any, else the bundled `public/examples/*.png` — add 4 PNGs from `/var/lib/printtweak/failed/**/view-*-front.png` is NOT allowed (customer data); use the retained knob/trophy/trinket/netgate renders only if the brain provides them under `public/examples/`; otherwise a neutral placeholder tile). Three "01/02/03" cards, "Real parts, real times" grid (4 cards with the numbers on the canvas), beta pricing band, footer. Keep `RecordVisit`.
2. **New part `app/design/new/page.tsx`** = `Request.dc.html`: stage with the form on the left (textarea, exact-text field, three "Try one" chips that fill the textarea, Design it button, helper line), mascot right with the hint card. Keep the upload and rights checkbox from the current form (place them under the exact-text row, same input style). Keep the existing mutations and routing.
3. **Working page `app/design/[id]/page.tsx`** = `Main.dc.html` while `status` is queued/designing/checking: heading "Building your <noun>" (noun = first noun-ish word of the request, fallback "part"), mono line `version N · mm:ss · usually X–Y min` (from `designs.get`: `versions.length`, `elapsedMs`, `medianDesignMs`), stage panel with mascot + tablet card showing `latestGlbUrl` in `ModelViewer` (free orbit) — when there is no version yet, the tablet shows the pulsing "waiting for the first version" state; version chips row (`v1 name`, current highlighted, next dashed) that switch the viewer to that version's `glbUrl`; right column: steps card (queued → designing → checking → ready, done = teal dot, current = pulsing accent dot, pending = outline), "What it's doing" card = last 4 `log` lines (replaces the SCRIPT·LIVE panel on the canvas — we do not stream code yet; label it "What it's doing"), estimate line. Below: "Change something while it works" composer (existing `addTweak`) and "Your request" card. Mobile: single column, stage 100% width, chips scroll horizontally.
4. **Ready page** (same route, `status === "ready"`) = `Ready.dc.html`: heading "Your <noun> is ready" + mono "checked · sliced · priced in N min", stage with mascot + tablet showing `glbUrl` with the two dials from the existing `TabletPreview` (keep its transform clip; the dials stay), version chips incl. "final"; right: quote card (pieces, per-part hours/grams, Download 3MF / Download STEP rows via `PartDownloads` data, accent "Print + post £19 · 3–5 days" row that stays disabled/"coming soon" while `printPricePence` is absent), "Change something" composer, "What happened (N steps)" `<details>` with the full log + messages.
5. **Failed / declined**: keep current wording (`lib/messages.ts`), restyle to the system (oops mascot, card).

## Tests and proof
- Existing vitest/jsdom page tests updated, plus: landing renders hero + CTA; new-part chips fill the textarea; working page shows version chips from `versions` and switches the viewer url; ready page shows per-part download rows and the disabled print row; failed state text unchanged.
- `run-limited npx vitest run`, `npx tsc --noEmit`, `npx next build` (run-limited) green.
- Screens: `node scripts/snap.mjs`-style captures are not set up in this repo — use Playwright (`@playwright/test` is a dev dep) to screenshot `/`, `/design/new`, and `/design/<id>` in working and ready states with a mocked Convex (or the test account state at `/tmp/claude-0/-root-3d-printing/b59e9ac3-f8b5-4a83-98d1-e5d6fbb04e3c/scratchpad/pt-test-state.json` against a local `next dev` pointed at the dev Convex, if configured) at 1280 and 390 wide; save under `spec/screens/`. The brain runs `design-gate` on the deployed preview.

## Hard rules
- No self-review, merge or push. Report RED/GREEN, file:line, screenshots' paths, and anything not verified.
- `versions`/`latestGlbUrl` come from part 1 (`vault/Plans/2026-09-16-tweakmypart-live-versions-plan.md`); if that PR is not merged yet, code against the contract and treat a missing field as `[]`/`null`.
- No new UI libraries. Tailwind 4 + globals tokens only. Keep every input labelled, focus visible, contrast ≥ 4.5:1 (muted text on card passes; dim `#5f6773` only for non-essential labels).
